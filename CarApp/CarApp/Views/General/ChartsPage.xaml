<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:microcharts="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             x:Class="CarApp.Views.ChartsPage"
             Title="Διάγραμμα"
             Shell.ForegroundColor="Transparent">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="charts" />
    </ContentPage.ToolbarItems>
    <StackLayout>

        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <StackLayout Orientation="Horizontal" Spacing="25">
                <Image Source="charts" HeightRequest="60"/>
                <Label Text="Έξοδα Αυτοκινήτου" TextColor="White" FontSize="34" VerticalTextAlignment="End"/>
            </StackLayout>
        </Frame>

        <ScrollView>
            <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="Center" >
                <microcharts:ChartView x:Name="chartViewBar" HeightRequest="600"/>
            </StackLayout>
        </ScrollView>
        <Button VerticalOptions="EndAndExpand" Text="Αναλυτικά" Command="{Binding CarCommand}"/>
    </StackLayout>
</ContentPage>